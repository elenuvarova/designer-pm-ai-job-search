PORT=3001
NODE_ENV=development

# Database — leave blank locally (uses SQLite automatically).
# In production, set DATABASE_URL to the Coolify-internal Postgres connection
# string (no SSL flag needed):
#   postgres://postgres:PASSWORD@CONTAINER_NAME:5432/postgres
# (If you ever point at an external Postgres that requires SSL, append
#  ?sslmode=require — SSL is enabled automatically only when the URL contains it.)
# DATABASE_URL=

# Adzuna — the only free source with native BE + NL + LU coverage.
# Get a free instant key at https://developer.adzuna.com/signup
ADZUNA_APP_ID=
ADZUNA_APP_KEY=

# Gemini AI — primary LLM for fit scoring + RAG (1,500 req/day free).
# Get a free key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Groq — fallback LLM. Get a free key at https://console.groq.com
GROQ_API_KEY=

# The Muse — optional supplemental source.
# Get a free key at https://www.themuse.com/developers/api/v2
THE_MUSE_API_KEY=
